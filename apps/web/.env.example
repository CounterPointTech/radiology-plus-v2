NEXT_PUBLIC_API_URL=http://localhost:5171
NEXT_PUBLIC_SIGNALR_URL=http://localhost:5171/hubs

# Dev/ops helpers (NRS "Project now" button, etc.). Off by default — set to
# "true" only on dev machines that have access to the /dev/refresh endpoint.
NEXT_PUBLIC_SHOW_DEV_TOOLS=true
